from .pd_df import pd_df_pipeline
# TODO: add all pipelines that should be exposed to the user of your backend in the import statement above.

pipelines = {
    "pd_df_pipeline": pd_df_pipeline,   # TODO: adapt identifier to something approproiate
}